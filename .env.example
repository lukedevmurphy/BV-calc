# Neon Postgres connection string (pooled). Required only for saving/loading
# proposals — the builder, live recompute, and PPTX export all work without it.
# Example: postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require
DATABASE_URL=
